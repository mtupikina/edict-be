# Agent instructions (edict-be)

This directory is the **edict-be** NestJS app (its own git repo).

## Before coding

1. Read **workspace** `.cursor/rules/` (`apply-project-rules.mdc`, `two-separate-repos.mdc`).
2. Read **all** rules in **`edict-be/.cursor/rules/`** (including `agent-workflow.mdc`).

## Commands

Run npm/git **from `edict-be/`**, not the workspace root.
